# coinDataMinning v3 — 제품 요구사항 정의서 (PRD)

| 항목 | 내용 |
|---|---|
| 문서 버전 | 1.0 |
| 작성일 | 2026-09-24 |
| 대상 시스템 | 바이낸스 USD-M 무기한 선물 ETH/USDT 판단 재료 생성기 |
| 선행 버전 | coinDataMinning v2.1.3 |

---

## 1. 문서 목적과 범위

### 1.1 이 문서가 정의하는 것

- 시스템이 해결하려는 문제와 성공/실패 판정 기준
- 프로그램·LLM·사용자의 역할 경계
- 사용자 플로우와 시스템 아키텍처
- 데이터 소스별 취득 경로와 제약
- 저장소 스키마, 지표 명세, 출력 명세
- 기능 요구사항과 비기능 요구사항
- 이번 버전의 범위 밖 항목과 그 이유

### 1.2 이 문서가 정의하지 않는 것

- 매매 전략, 진입/청산 규칙, 포지션 사이징
- LLM 프롬프트 본문 (별도 문서에서 정의)
- 구현 언어의 세부 코딩 규약 (CLAUDE.md에서 정의)

### 1.3 읽는 순서

2장에서 문제를, 3~5장에서 목표와 경계를, 6~9장에서 설계를, 10~12장에서 요구사항을 정의한다. 구현자는 10장(기능 요구사항)과 11장(비기능 요구사항)을 작업 단위의 기준으로 삼는다.

---

## 2. 배경과 문제 정의

### 2.1 선행 버전의 구조

v2.1.x는 바이낸스 REST API에서 지표를 수집해 JSON으로 출력하고, 사용자가 이를 LLM에 붙여넣어 판단을 받는 구조였다. JSON은 원시 지표값과 OHLCV 시계열로 구성되었고, 해석과 계산 모두를 LLM이 수행했다.

### 2.2 확인된 네 가지 결함

**P-1. 계산 주체 오류**
가격과 지표 간 거리 계산, 확률 추정 등을 LLM이 수행했다. LLM의 산술 결과는 검증되지 않으며, 확률값은 근거 없이 생성되었다. 동일 입력에 대한 출력이 재현되지 않는다.

**P-2. 상태 부재**
각 판단이 독립적으로 수행되어 직전 판단과의 연속성이 없었다. 동일 국면에서 판단이 반복적으로 반전되었다.

**P-3. 사후 확인형 트리거**
판단 기준이 "조건 충족이 확인된 후 행동"으로 설계되어, 확인 시점에는 이미 가격이 이동한 상태였다.

**P-4. 데이터 주기 불일치**
펀딩비는 8시간 주기, 미결제약정은 24시간 변화율만 제공되어 30분~5시간 보유 구간의 판단에 사용할 수 없었다. 연속된 다수 스냅샷에서 동일 값이 반복 출력되었다.

**P-5. 지표 간 정보 중복**
RSI·MACD·볼린저밴드·이동평균은 모두 종가에서 파생되어 상호 상관이 높다. "3개 지표 정렬"이 실질적으로 단일 신호의 중복 계수였다.

### 2.3 결함과 요구사항의 대응

| 결함 | 대응 요구사항 |
|---|---|
| P-1 | 모든 수치 계산을 프로그램이 수행 (FR-3.x) |
| P-2 | 요약에 직전 판단 상태를 포함 (FR-4.4) |
| P-3 | 이벤트에 발생 여부뿐 아니라 진행 정도를 포함 (FR-3.6) |
| P-4 | 프리미엄 인덱스 1분, 미결제약정 5분 주기 사용 (FR-1.2, FR-1.3) |
| P-5 | 지표를 계열로 분류하여 출력 (FR-4.2) |

---

## 3. 목표와 비목표

### 3.1 목표

- **G-1**: 판단 시점의 시장 상태를 구조화된 JSON으로 산출한다.
- **G-2**: 모든 수치를 프로그램이 계산하여 LLM의 산술 수행을 제거한다.
- **G-3**: 데이터의 결손과 갱신 시각을 명시하여 신뢰도 판단을 가능하게 한다.
- **G-4**: 이 구조가 기존 방식보다 유용한지 검증할 수 있는 최소 기능을 제공한다.

### 3.2 비목표

- **NG-1**: 매매 신호 생성. 시스템은 재료만 제공하며 방향을 제시하지 않는다.
- **NG-2**: 주문 실행. 거래소 계정 API를 사용하지 않는다.
- **NG-3**: 수익 최적화. 파라미터를 성과에 맞춰 조정하는 기능을 포함하지 않는다.
- **NG-4**: 실시간 상시 가동. 스케줄 실행과 수동 실행만 지원한다.
- **NG-5**: 알림. 이벤트 감지 후 사용자에게 통지하는 기능을 포함하지 않는다.

---

## 4. 검증 기준

### 4.1 검증 방법

시스템 완성 후 2~3주간 수동 요약 경로를 사용하여 판단을 수행하고, 아래 기준으로 계속 여부를 결정한다.

### 4.2 계속 판정 조건

- 요약 JSON만으로 시장 상태 파악이 가능하다.
- LLM 출력에서 근거 없는 수치가 발생하지 않는다.
- 동일 국면에서 판단의 일관성이 유지된다.
- 요약 생성부터 판단까지 소요 시간이 차트 직접 확인보다 짧다.

### 4.3 중단 판정 조건

- 요약 확인 후에도 차트를 별도 확인해야 한다.
- 요약에 누락된 정보를 수동으로 보완해야 하는 경우가 반복된다.
- 판단 결과가 실행 가능한 형태로 수렴하지 않는다.

### 4.4 검증 대상이 아닌 것

매매 성과는 이번 버전의 검증 대상이 아니다. 판단 적중률 측정은 판단 로그와 결과 라벨링이 구현된 이후 단계에서 수행한다.

---

## 5. 역할 경계

### 5.1 역할별 책임

| 주체 | 책임 | 산출물 |
|---|---|---|
| 프로그램 | 데이터 수집, 지표 계산, 이벤트 판정, 구조화 | JSON 요약 |
| LLM | 사실 간 관계 해석, 신호 충돌 중재, 판단 | 텍스트 판단 |
| 사용자 | 실행 결정, 주문 집행, 규칙 결정 | — |

### 5.2 경계 원칙

- **B-1**: 수치는 프로그램만 생성한다. LLM은 프로그램이 산출한 수치를 인용하며, 새로운 수치를 만들지 않는다.
- **B-2**: 프로그램은 방향을 제시하지 않는다. 이벤트 발생 여부와 측정값만 출력하며, 매수/매도 판정을 포함하지 않는다.
- **B-3**: 실행 권한은 단방향이다. 프로그램과 LLM은 거래소에 대한 쓰기 권한을 갖지 않는다.
- **B-4**: 검증된 판단 규칙은 코드로 이관한다. LLM이 반복 수행하는 판단 중 검증된 것은 후속 버전에서 프로그램 계산으로 전환한다.

---

## 6. 사용자 플로우

### 6.1 UF-1: 초기화

**목적**: 저장소를 최초 구축한다.

**입력**: 적재 기간 (설정 파일 기본값 사용 가능)

**처리**
1. 저장소 스키마 생성
2. 아카이브에서 지정 기간의 일별 파일 다운로드 (klines, premiumIndexKlines, metrics)
3. 파일별 체크섬 검증, 압축 해제, 파싱, 시간순 정렬
4. 저장소에 적재 (중복은 기본키로 차단)
5. 아카이브 미제공 구간(최근 1~2일)을 REST로 보완
6. 결측 구간을 탐지하여 기록

**출력**: 적재 결과 요약 (데이터셋별 행 수, 기간, 결측 구간 수)

**실패 처리**: 중단 후 재실행 시 이미 적재된 구간을 건너뛰고 이어서 수행한다.

### 6.2 UF-2: 스케줄 수집

**목적**: 보관 기간이 제한된 데이터의 유실을 방지한다.

**실행**: OS 스케줄러에 의한 자동 실행 (하루 수 회)

**처리**
1. 데이터셋별 마지막 적재 시각 조회
2. 해당 시각 이후 구간을 REST로 요청
3. 저장소에 적재
4. 결측 탐지 및 기록
5. 실행 이력 기록

**출력**: 없음 (로그만 기록)

**실패 처리**: 실패를 로그에 기록하고 종료한다. 다음 실행이 미적재 구간을 보완한다.

**근거**: 미결제약정과 롱숏 비율은 REST로 최근 30일만 조회 가능하며, 체결 데이터는 24시간만 조회 가능하다. 아카이브는 1~2일 지연되어 공개되므로, 그 사이 구간은 주기 수집으로만 확보된다.

### 6.3 UF-3: 수동 요약 생성

**목적**: 판단 시점의 시장 상태를 산출한다.

**입력**: 없음 (실행 명령만)

**처리**
1. 최근 구간을 REST로 요청하여 저장소 갱신
2. 계산 대상 구간을 저장소에서 로드
3. 1분봉으로 상위 타임프레임 봉 합성
4. 지표·구조·레짐·레벨 계산
5. 이벤트 판정
6. 직전 요약 정보 조회
7. JSON 요약 조립 및 파일 저장

**출력**: JSON 파일 경로, 요약 ID, 기준 시각

**실패 처리**: 일부 데이터 취득 실패 시에도 요약을 생성하며, 결손을 요약 내에 명시한다.

### 6.4 UF-4: 상태 점검

**목적**: 저장소의 적재 상태와 결손을 확인한다.

**입력**: 없음

**처리**: 데이터셋별 적재 기간, 행 수, 결측 구간, 최종 갱신 시각을 조회한다.

**출력**: 상태 보고 (표준 출력)

---

## 7. 시스템 아키텍처

### 7.1 계층 구조

```
[4] report   요약 조립, JSON 직렬화
                    ↑
[3] compute  지표, 구조, 레짐, 레벨, 이벤트 판정
                    ↑
[2] store    저장소 읽기/쓰기, 결측 관리
                    ↑
[1] ingest   아카이브 다운로드, REST 클라이언트
                    ↑
             바이낸스 (아카이브 / REST API)
```

### 7.2 의존 방향 규칙

- 각 계층은 직하위 계층에만 의존한다.
- 상위 계층을 참조하지 않는다.
- `compute`는 데이터의 출처(아카이브/REST/실시간)를 인지하지 않는다.
- `ingest`는 지표 정의를 인지하지 않는다.

**근거**: 후속 버전에서 실시간 수집기를 추가할 때 `ingest`와 `store`만 확장하면 되며, `compute` 이상은 변경되지 않는다.

### 7.3 확장 지점

| 후속 기능 | 삽입 계층 |
|---|---|
| 실시간 체결/청산 수집 | ingest, store |
| 판단 로그 및 결과 라벨링 | store, report |
| base rate 통계 | compute (신규 모듈) |
| 이벤트 감지 및 알림 | compute와 report 사이 |

---

## 8. 데이터 소스 명세

### 8.1 취득 경로 요약

| 데이터 | 과거 (아카이브) | 최근 (REST) |
|---|---|---|
| 1분봉 | `futures/um/daily/klines` | `/fapi/v1/klines` |
| 프리미엄 인덱스 | `futures/um/daily/premiumIndexKlines` | `/fapi/v1/premiumIndexKlines` |
| 미결제약정·롱숏 비율 | `futures/um/daily/metrics` | `/futures/data/openInterestHist` 외 |
| 체결 (T2) | `futures/um/daily/aggTrades` | `/fapi/v1/aggTrades` |
| 청산 | 취득 불가 | 취득 불가 |

아카이브 기준 주소: `https://data.binance.vision/`

### 8.2 REST API 제약

| 엔드포인트 | 가중치 | 제약 |
|---|---|---|
| `/fapi/v1/klines` | limit 비례 | limit 최대 1000 |
| `/fapi/v1/premiumIndexKlines` | limit 비례 | limit 최대 1000 |
| `/futures/data/openInterestHist` | 0 | period 5m 이상, limit 최대 500, **최근 30일만 제공**, IP당 5분간 1000회 |
| `/futures/data/topLongShortPositionRatio` | 0 | period 5m 이상, 최근 30일 |
| `/futures/data/topLongShortAccountRatio` | 0 | 동일 |
| `/futures/data/globalLongShortAccountRatio` | 0 | 동일 |
| `/fapi/v1/aggTrades` | 20 | **24시간 이내만 조회 가능**, startTime/endTime 동시 지정 시 간격 1시간 미만, limit 최대 1000 |

**전역 제약**: USD-M 선물 REST는 IP당 분당 요청 가중치 2400.

### 8.3 aggTrades의 구조적 특성

공식 문서에 따르면 `/fapi/v1/aggTrades`는 시장 체결만 집계하며, **보험기금 체결과 ADL 체결은 포함하지 않는다**.

**영향**: 공개 체결량은 오프북으로 처리된 강제 청산 물량을 구조적으로 제외한다. 따라서 미결제약정 감소분과 공개 체결량의 괴리는 오프북 청산 규모의 간접 지표로 사용할 수 있다. 이 로직은 T2에서 구현한다.

### 8.4 아카이브 파일 특성

**metrics 데이터셋**
- 컬럼: `create_time`, `symbol`, `sum_open_interest`, `sum_open_interest_value`, `count_toptrader_long_short_ratio`, `sum_toptrader_long_short_ratio`, `count_long_short_ratio`, `sum_taker_long_short_vol_ratio`
- 간격: 5분
- **행이 시간순으로 정렬되어 있지 않다.** 적재 전 정렬이 필수다.
- **결측 행이 존재한다.** 1일 288행이 기대값이나 실제로는 미달한다.

**premiumIndexKlines 데이터셋**
- 구조는 klines와 동일하나 `volume`, `quote_volume`, `taker_buy_volume`, `taker_buy_quote_volume`이 0이다.
- 사용 컬럼은 `open`, `high`, `low`, `close`에 한정한다.
- `count` 필드는 1분 내 샘플 수(관측값 12)를 의미한다.
- 값은 프리미엄 비율이며 음수를 가질 수 있다.

**공통**
- 시각 컬럼은 밀리초 정수다. 스프레드시트로 열면 지수 표기로 손실된다.
- 각 파일에 `.CHECKSUM` 동반 파일이 존재한다.
- 공개 시점은 해당 UTC 일자 종료 이후이며, 1~2일 지연을 가정한다.

### 8.5 청산 데이터 취득 불가

| 경로 | 상태 |
|---|---|
| 아카이브 `liquidationSnapshot` (USD-M) | 2024-03-31 이후 미공개 |
| REST `allForceOrders` | 인증 필요 |
| WebSocket `@forceOrder` | 실시간 전용, 소급 불가 |

**결론**: 청산 데이터는 실시간 수집을 시작한 시점 이후만 존재하며 과거 복원이 불가능하다. 이번 버전은 청산 없이 동작하며, 청산 의존 로직(유동성 사냥 3조건 판정, 청산 밀도)은 범위에서 제외한다.

### 8.6 시각 기준

- 모든 시각은 UTC 밀리초 정수로 저장한다.
- 표시가 필요한 경우에만 변환한다.
- 시스템 시계 오차는 `/fapi/v1/time`으로 확인 가능하나, 이번 버전에서는 보정하지 않고 기록만 한다.

---

## 9. 저장소 스키마

### 9.1 저장소 선택

SQLite 단일 파일을 사용한다.

**근거**: 결측 탐지, 중복 차단, 갱신 주기가 다른 데이터셋의 시각 기준 결합이 필요하다. 파일 기반 저장은 이 세 가지를 매번 전체 스캔으로 처리해야 한다. 서버형 RDBMS는 운영 부담 대비 이점이 없다.

**예외**: 1초 버킷 체결 데이터(T2)는 행 수가 크고 추가 전용이므로, 구현 시점에 컬럼 지향 파일 형식으로 분리할지 재검토한다.

### 9.2 테이블 정의

**kline_1m**

| 컬럼 | 타입 | 비고 |
|---|---|---|
| symbol | TEXT | 복합 기본키 |
| open_time | INTEGER | 복합 기본키, UTC ms |
| open, high, low, close | REAL | |
| volume | REAL | |
| quote_volume | REAL | |
| trade_count | INTEGER | |
| taker_buy_volume | REAL | |
| taker_buy_quote_volume | REAL | |
| source | TEXT | `archive` 또는 `rest` |
| ingested_at | INTEGER | UTC ms |

**premium_index_1m**

| 컬럼 | 타입 | 비고 |
|---|---|---|
| symbol | TEXT | 복합 기본키 |
| open_time | INTEGER | 복합 기본키 |
| open, high, low, close | REAL | 프리미엄 비율 |
| sample_count | INTEGER | 원본 `count` |
| source | TEXT | |
| ingested_at | INTEGER | |

**metrics_5m**

| 컬럼 | 타입 | 비고 |
|---|---|---|
| symbol | TEXT | 복합 기본키 |
| ts | INTEGER | 복합 기본키 |
| sum_open_interest | REAL | 계약 수 |
| sum_open_interest_value | REAL | 명목 가치 |
| top_position_ratio | REAL | |
| top_account_ratio | REAL | |
| global_account_ratio | REAL | |
| taker_buy_sell_ratio | REAL | |
| source | TEXT | |
| ingested_at | INTEGER | |

**data_gap**

| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | INTEGER | 기본키 |
| dataset | TEXT | |
| symbol | TEXT | |
| start_ms, end_ms | INTEGER | 결측 구간 |
| reason | TEXT | `archive_missing`, `rest_failed`, `source_gap`, `retention_expired` |
| detected_at | INTEGER | |
| resolved_at | INTEGER | NULL이면 미해소 |

**ingest_run**

| 컬럼 | 타입 | 비고 |
|---|---|---|
| id | INTEGER | 기본키 |
| mode | TEXT | `init`, `schedule`, `manual` |
| started_at, finished_at | INTEGER | |
| status | TEXT | `success`, `partial`, `failed` |
| detail | TEXT | JSON 문자열 |

**summary_log**

| 컬럼 | 타입 | 비고 |
|---|---|---|
| summary_id | TEXT | 기본키 |
| created_at | INTEGER | |
| trigger | TEXT | `manual`, `scheduled` |
| ref_price | REAL | |
| file_path | TEXT | |

### 9.3 스키마 원칙

- 모든 시계열 테이블은 `(symbol, 시각)` 복합 기본키를 가진다. 중복 적재는 기본키 충돌로 차단된다.
- `source` 컬럼으로 아카이브와 REST의 출처를 구분한다.
- 결측은 행을 생성하지 않고 `data_gap`에 기록한다. **보간값을 저장하지 않는다.**
- 단일 종목 운용이나 `symbol` 컬럼을 유지하여 확장 여지를 남긴다.

---

## 10. 기능 요구사항

### 10.1 수집 (ingest)

**FR-1.1 아카이브 다운로드**
지정 기간의 일별 아카이브 파일을 다운로드하고 `.CHECKSUM`으로 무결성을 검증한다. 검증 실패 시 해당 파일을 폐기하고 재시도하며, 재시도 실패 시 `data_gap`에 기록한다.

**FR-1.2 아카이브 파싱**
압축 해제 후 CSV를 파싱한다. metrics는 정렬되지 않은 상태로 제공되므로 시각 기준 오름차순 정렬 후 적재한다. 시각 컬럼은 정수로 파싱하며 부동소수점 변환을 거치지 않는다.

**FR-1.3 REST 수집 — 봉 데이터**
`/fapi/v1/klines`와 `/fapi/v1/premiumIndexKlines`에서 마지막 적재 시각 이후 구간을 요청한다. 요청당 최대 1000건 제한에 따라 분할 요청한다.

**FR-1.4 REST 수집 — 파생 지표**
`/futures/data/openInterestHist`, `topLongShortPositionRatio`, `topLongShortAccountRatio`, `globalLongShortAccountRatio`에서 5분 주기 데이터를 요청하여 `metrics_5m`에 통합 적재한다. 각 엔드포인트의 응답 시각이 일치하지 않을 수 있으므로 시각 기준 결합한다.

**FR-1.5 요청 가중치 제어**
응답 헤더의 사용 가중치를 추적하여 분당 한도(2400) 초과를 방지한다. 한도의 설정 가능한 비율(기본 70%)에 도달하면 요청을 지연한다. HTTP 429 수신 시 `Retry-After`를 준수한다.

**FR-1.6 재시도**
네트워크 오류와 5xx 응답에 대해 지수 백오프로 재시도한다. 최대 재시도 횟수는 설정값을 따른다. 4xx(429 제외)는 재시도하지 않는다.

**FR-1.7 경계 처리**
아카이브와 REST의 취득 구간이 겹칠 경우 양쪽 모두 적재를 시도하되, 기본키 충돌 시 기존 행을 유지한다.

### 10.2 저장 (store)

**FR-2.1 멱등 적재**
동일 데이터를 반복 적재해도 결과가 변하지 않는다.

**FR-2.2 결측 탐지**
적재 후 데이터셋별 기대 간격(1분, 5분)에 대해 누락 시각을 산출하여 `data_gap`에 기록한다. 이미 기록된 결측이 후속 적재로 해소되면 `resolved_at`을 갱신한다.

**FR-2.3 결측 비보간**
결측 구간에 대해 보간, 전방 채우기, 기본값 삽입을 수행하지 않는다.

**FR-2.4 적재 이력**
모든 수집 실행을 `ingest_run`에 기록한다.

**FR-2.5 상태 조회**
데이터셋별 적재 기간, 행 수, 미해소 결측 구간, 최종 갱신 시각을 조회하는 인터페이스를 제공한다.

### 10.3 계산 (compute)

**FR-3.1 봉 합성**
1분봉으로 15분·30분·1시간·1일 봉을 합성한다. 합성 구간 내 결측이 있으면 해당 봉에 결손 표시를 부여한다. 진행 중인 미완성 봉은 완성 봉과 구분한다.

**FR-3.2 변동성 지표**
ATR(기본 14)과 Parkinson 변동성(기본 20)을 타임프레임별로 산출한다. Parkinson 변동성의 백분위 순위를 설정된 룩백(기본 100) 기준으로 산출한다.

**FR-3.3 스윙 구조**
ATR 기반 ZigZag로 스윙 고점·저점을 식별한다. 반전 임계값은 `k × ATR`이며 `k`는 설정값(기본 2.0)이다. 확정된 극점과 미확정 진행 파동을 분리하여 출력한다.

**FR-3.4 구조 전환**
직전 확정 스윙 극점 대비 종가 기준 돌파를 BOS로, 추세 방향의 반대 극점 돌파를 MSS로 판정한다. MSS는 돌파 봉의 몸통이 최근 N봉(기본 20) 평균 몸통의 배수(기본 1.5) 이상일 때만 성립한다.

**FR-3.5 되돌림**
확정된 직전 파동에 대해 되돌림 깊이(0~1 정규화)와 시간 비율(조정 봉 수 / 동인 파동 봉 수)을 산출한다.

**FR-3.6 캔들 구조**
봉별 위꼬리·아래꼬리·몸통 비율과, 몸통 크기의 ATR 대비 배수를 산출한다. 최근 N봉(설정값) 분량을 출력한다.

**FR-3.7 레짐 판정**
Kaufman Efficiency Ratio(기본 10)와 Parkinson 백분위를 산출하고, 설정된 임계값에 따라 `trend`, `range`, `transition` 중 하나로 분류한다. 각 타임프레임별로 상태와 지속 봉 수를 출력한다.

**FR-3.8 충격 상태 판정**
캔들 꼬리 비율이 임계값을 초과하거나 1~2봉 내 반대 방향 MSS가 발생한 경우 `shock` 상태로 판정하고, FR-3.7의 레짐 판정을 덮어쓴다. `shock` 상태에서는 레짐 확신도를 산출하지 않는다.

**FR-3.9 파생 지표**
프리미엄 인덱스의 최근 변화율과 부호 전환 여부를 산출한다. 계약 수 기준 미결제약정의 변화와 가격 변화를 결합해 4분면 상태를 판정한다. **명목 미결제약정을 방향성 판단에 사용하지 않는다.**

**FR-3.10 레벨 후보 산출**
확정 스윙 극점, VWAP, 24시간 고저에서 레벨 후보를 도출한다. 각 레벨은 `[중심 ± w × ATR]` 구간으로 표현하며 `w`는 설정값(기본 0.25)이다. 중심 간 거리가 설정값(기본 0.5 ATR) 미만인 레벨은 병합하고 병합된 근거 수를 기록한다.

**FR-3.11 레벨 강도 미산출**
이번 버전은 레벨 강도 점수를 산출하지 않는다. 레벨 목록과 근거 종류, 정규화 거리만 출력한다.

**FR-3.12 이벤트 판정**
이벤트는 발생 여부와 함께 측정값을 갖는다. 최소 공통 필드는 발생 시각, 경과 봉 수, 관련 타임프레임이며, 유형별로 침투 깊이(ATR 배수), 거래량 배수, 지속 봉 수를 포함한다.

**FR-3.13 계열 분류**
모든 지표와 이벤트는 `price_structure`, `regime`, `derivatives`, `level` 중 하나의 계열로 분류되어 출력된다.

**FR-3.14 확률 미산출**
시스템은 확률, 승률, 기대값을 산출하지 않는다. 해당 필드는 통계 기능 구현 이전까지 존재하지 않는다.

### 10.4 요약 (report)

**FR-4.1 요약 구조**
요약 JSON은 다음 섹션으로 구성된다.

| 섹션 | 내용 |
|---|---|
| `meta` | 요약 ID, 생성 시각, 트리거 종류, 종목, 기준 가격, 스키마 버전 |
| `data_freshness` | 데이터셋별 최종 갱신 시각과 기준 시각 대비 경과 |
| `price_structure` | 스윙, BOS/MSS, 되돌림, 캔들 구조 |
| `regime` | 타임프레임별 레짐 상태, 지속 봉 수, 충격 여부 |
| `derivatives` | 프리미엄 인덱스, 계약 수 OI와 4분면, 롱숏 비율 |
| `levels` | 레벨 목록, 구간, 근거, 정규화 거리 |
| `events` | 판정된 이벤트 목록과 측정값 |
| `state` | 직전 요약 ID와 생성 시각, 직전 대비 주요 변화 |
| `statistics` | 통계 미구현 표시 |
| `gaps` | 계산 구간 내 결측과 취득 실패 |
| `unavailable` | 이번 버전에서 제공되지 않는 데이터와 사유 |

**FR-4.2 결손 표기**
값이 없는 경우와 값이 0인 경우를 구분한다. 없는 값은 `null`로 표기하고 `gaps` 또는 `unavailable`에 사유를 기록한다.

**FR-4.3 갱신 시각 표기**
각 데이터 출처의 최종 갱신 시각을 요약에 포함한다. 갱신 시각이 기준 시각과 설정값 이상 차이나면 `data_freshness`에 경고 표시를 부여한다.

**FR-4.4 상태 연속성**
직전 요약의 ID와 생성 시각을 포함한다. 직전 요약이 존재하면 주요 상태값(레짐, 구조, 4분면)의 변화 여부를 기록한다.

**FR-4.5 요약 저장**
생성된 요약을 파일로 저장하고 `summary_log`에 기록한다. 파일명은 요약 ID를 포함한다.

**FR-4.6 부분 실패 시 생성**
일부 데이터 취득에 실패해도 요약을 생성한다. 실패한 데이터의 필드는 `null`이며 `gaps`에 사유가 기록된다.

### 10.5 인터페이스 (CLI)

**FR-5.1 명령 구성**
`init`, `sync`, `summary`, `status` 네 개의 하위 명령을 제공한다.

| 명령 | 대응 플로우 |
|---|---|
| `init` | UF-1 초기화 |
| `sync` | UF-2 스케줄 수집 |
| `summary` | UF-3 수동 요약 |
| `status` | UF-4 상태 점검 |

**FR-5.2 종료 코드**
정상 종료 0, 부분 실패 1, 실행 불가 2를 반환한다. 스케줄러가 결과를 판별할 수 있어야 한다.

**FR-5.3 진행 표시**
`init`은 장시간 실행되므로 진행 상황을 출력한다. `sync`는 비대화형 실행을 전제로 출력을 최소화한다.

### 10.6 설정

**FR-6.1 설정 파일**
모든 파라미터를 단일 설정 파일에서 관리한다. 섹션 구분: `data`, `indicators`, `regime`, `levels`, `events`, `report`, `runtime`.

**FR-6.2 기본값**
모든 설정 항목은 기본값을 가지며, 설정 파일 없이도 실행 가능하다.

**FR-6.3 하드코딩 금지**
지표 기간, 임계값, 배수, 룩백 등 조정 가능한 수치를 코드에 직접 기술하지 않는다.

**FR-6.4 설정 기록**
요약 생성 시 사용된 주요 파라미터를 `meta`에 포함한다. 파라미터 변경 전후의 요약을 구분할 수 있어야 한다.

---

## 11. 비기능 요구사항

### 11.1 정확성

**NFR-1.1** 지표 계산은 동일 입력에 대해 동일 출력을 산출한다.
**NFR-1.2** 시각 연산에서 부동소수점을 사용하지 않는다. 모든 시각은 정수 밀리초다.
**NFR-1.3** 결측 데이터가 계산에 포함될 경우 해당 지표에 결손 표시를 부여하며, 결측을 0 또는 직전값으로 대체하지 않는다.
**NFR-1.4** 합성 봉은 원본 1분봉의 결측 여부를 승계한다.

### 11.2 성능

**NFR-2.1** `summary` 실행은 정상 조건에서 30초 이내에 완료한다.
**NFR-2.2** `sync` 실행은 정상 조건에서 60초 이내에 완료한다.
**NFR-2.3** `init`의 소요 시간은 제한하지 않되, 중단 후 재개가 가능해야 한다.
**NFR-2.4** 계산 대상 구간은 설정값으로 제한하며, 전체 저장소를 스캔하지 않는다.

### 11.3 신뢰성

**NFR-3.1** 부분 실패 시 시스템은 사용 가능한 결과를 산출한다.
**NFR-3.2** 수집 중단 시 저장소는 일관된 상태를 유지한다. 부분 적재된 파일이 무결성 검증 실패로 남지 않는다.
**NFR-3.3** 동일 명령의 반복 실행이 데이터를 손상시키지 않는다.
**NFR-3.4** 외부 API 장애가 저장소 무결성에 영향을 주지 않는다.

### 11.4 관측성

**NFR-4.1** 모든 외부 요청은 엔드포인트, 파라미터 범위, 응답 상태, 소요 시간을 로그에 기록한다.
**NFR-4.2** 결측 탐지 결과는 로그와 저장소 양쪽에 기록한다.
**NFR-4.3** 로그 수준은 설정으로 조정 가능하다.
**NFR-4.4** `status` 명령으로 저장소 상태를 코드 수정 없이 확인할 수 있다.

### 11.5 확장성

**NFR-5.1** 신규 데이터 소스 추가 시 `compute` 이상 계층을 변경하지 않는다.
**NFR-5.2** 신규 지표 추가 시 기존 지표 계산을 변경하지 않는다.
**NFR-5.3** 종목 확장 시 스키마를 변경하지 않는다.
**NFR-5.4** 요약 스키마에 버전 필드를 포함하여 후속 변경을 식별 가능하게 한다.

### 11.6 유지보수성

**NFR-6.1** 계층 간 의존 방향을 위반하는 참조를 금지한다.
**NFR-6.2** 외부 API의 제약값(한도, 보관 기간, 요청 간격)은 상수 또는 설정으로 명시하며 코드 내에 산재시키지 않는다.
**NFR-6.3** 의존 패키지는 최소화한다.

### 11.7 보안

**NFR-7.1** 인증이 필요한 엔드포인트를 사용하지 않는다. API 키를 취급하지 않는다.
**NFR-7.2** 시스템은 거래소에 대한 쓰기 요청을 수행하지 않는다.

### 11.8 이식성

**NFR-8.1** 저장소는 단일 파일로 이동 가능하다.
**NFR-8.2** 설정 파일과 저장소 경로는 설정으로 지정 가능하다.
**NFR-8.3** OS 스케줄러에 의한 비대화형 실행을 지원한다.

### 11.9 테스트 가능성

**NFR-9.1** 지표 계산은 외부 네트워크 없이 고정 입력으로 검증 가능하다.
**NFR-9.2** 수집 계층은 응답을 대체 가능한 형태로 구성하여 네트워크 없이 검증 가능하다.
**NFR-9.3** 결측·중복·부분 실패 상황을 재현하는 테스트를 포함한다.

---

## 12. 오류와 결측 처리 원칙

### 12.1 원칙

- **E-1**: 결손을 은폐하지 않는다. 취득 실패, 결측, 미구현을 각각 구분하여 표기한다.
- **E-2**: 결손을 추정값으로 대체하지 않는다.
- **E-3**: 부분 실패가 전체 실패로 확대되지 않는다.
- **E-4**: 실패 사유를 사용자가 식별 가능한 형태로 전달한다.

### 12.2 결손 분류

| 분류 | 의미 | 표기 |
|---|---|---|
| `retention_expired` | 보관 기간 경과로 취득 불가 | `gaps` |
| `archive_missing` | 아카이브 파일 미공개 | `gaps` |
| `source_gap` | 원본 데이터 자체의 결측 | `gaps` |
| `rest_failed` | 요청 실패 | `gaps` |
| `not_implemented` | 이번 버전 범위 밖 | `unavailable` |

---

## 13. 범위 밖 항목

| 항목 | 제외 사유 | 삽입 계층 |
|---|---|---|
| 1초 버킷 체결 집계 | REST 24시간 제약으로 대량 취득에 아카이브 연동이 선행되어야 함 | ingest, compute |
| 체결 기반 지표 (델타 EMA, 거래 강도, Kyle's lambda) | 체결 수집 선행 필요 | compute |
| 내재 청산 불일치 플래그 | 체결 수집 선행 필요 | compute |
| 청산 수집 및 청산 의존 지표 | 실시간 수집기 부재, 과거 복원 불가 | ingest |
| 거래량·OI 프로파일 | 레벨 강도 산출과 함께 구현 | compute |
| 레벨 강도 점수 | 과거 반응 이력 필요 | compute |
| MAE/MFE, base rate | 누적 데이터 필요 | compute (신규) |
| 판단 로그 및 결과 라벨링 | 검증 1단계 이후 | store, report |
| 이벤트 감지 및 알림 | 유효 이벤트 식별 이후 | compute/report 사이 |
| 실시간 수집기 | 별도 장비 구성 이후 | ingest |
| 레짐 확신도 합성, 통합 취약성 지수 | 가중치 검증 데이터 부재 | compute |

---

## 14. 완료 기준

이번 버전은 아래를 모두 충족할 때 완료로 판정한다.

1. `init` 실행으로 설정된 기간의 1분봉·프리미엄 인덱스·metrics가 적재되고, 결측이 `data_gap`에 기록된다.
2. `init` 중단 후 재실행 시 이어서 적재된다.
3. `sync` 실행으로 마지막 적재 시각 이후 구간이 추가되며, 반복 실행이 데이터를 손상시키지 않는다.
4. `summary` 실행으로 FR-4.1의 모든 섹션을 포함한 JSON이 생성된다.
5. 네트워크를 차단한 상태에서 `summary`를 실행하면, 저장된 데이터로 요약이 생성되고 `gaps`에 취득 실패가 기록된다.
6. `status` 실행으로 데이터셋별 적재 상태와 미해소 결측이 출력된다.
7. 설정 파일에서 ZigZag `k`값을 변경하면 스윙 결과와 `meta`의 파라미터 기록이 함께 변경된다.
8. 요약 JSON에 확률·승률·기대값 필드가 존재하지 않는다.
9. FR-3.x 지표에 대한 고정 입력 기반 테스트가 통과한다.

---

## 15. 미해결 사항과 리스크

### 15.1 파라미터 미검증

ZigZag의 `k=2.0`, ATR 기간, 레짐 임계값은 관행적 기본값이며 이 시장·타임프레임에서 검증되지 않았다. 검증 단계에서 조정 대상이다. 설정 분리(FR-6.3)로 조정 비용을 낮춘다.

### 15.2 아카이브 결측

metrics 아카이브에 원본 결측이 존재한다. 결측률이 높을 경우 5분 주기 파생 지표의 연속성이 저하된다. `status`로 결측률을 모니터링한다.

### 15.3 청산 부재의 영향

청산 데이터 없이는 급격한 가격 반전의 원인을 직접 관측할 수 없다. 이번 버전은 가격 기반 충격 판정(FR-3.8)으로 대체하며, 이 판정의 신뢰도는 검증되지 않았다.

### 15.4 미완성 봉 처리

판단 시점은 봉 마감과 일치하지 않는다. 진행 중인 봉의 지표값은 봉 마감 시 변경된다. 시스템은 이를 구분 표기하나, 해석 주체가 이를 고려해야 한다.

### 15.5 API 변경

외부 API 제약은 변경될 수 있다. 8.2의 제약값은 2026-09 기준이며, 변경 시 NFR-6.2에 따라 상수/설정 수정으로 대응한다.
