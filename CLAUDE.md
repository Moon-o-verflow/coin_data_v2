# CLAUDE.md

이 문서는 본 저장소에서 작업할 때 준수할 규칙을 정의한다. 요구사항의 근거와 상세는 `docs/PRD.md`를 참조한다. 두 문서가 충돌하면 `docs/PRD.md`가 우선한다.

---

## 1. 프로젝트 요약

바이낸스 USD-M 무기한 선물 ETH/USDT의 시장 상태를 구조화된 JSON으로 산출하는 CLI 프로그램이다. 산출된 JSON은 사용자가 LLM에 전달하여 판단 재료로 사용한다.

프로그램은 수치 계산과 이벤트 판정을 담당하고, 방향 제시와 확률 산출을 수행하지 않는다.

---

## 2. 절대 규칙

아래 항목은 예외 없이 적용한다. 위반이 불가피하다고 판단되면 구현을 중단하고 질문한다.

**R-1. 확률·승률·기대값을 산출하지 않는다.**
해당 필드를 요약 스키마에 추가하지 않는다.

- **예외**: 요약의 `statistics` 섹션은 과거 발생 빈도를 기술할 수 있다(`docs/PRD.md` 부록 B). 이때 표본 수(`n`)·원 건수·기간·정의 버전을 항상 함께 싣고, 표본이 `min_n` 미만이면 비율을 싣지 않으며, 결과 정의는 결과를 보기 전에 부록 B에 확정한다. 정의를 바꾸려면 `definition_version`을 올리고 부록 B에 사유를 기록한다.
- `statistics` 밖에서는 아래 이름을 키나 값에 쓰지 않는다. T-6은 이 목록으로 검사한다.
  `held_ratio`, `failed_ratio`, `probability`, `prob`, `win_rate`, `hit_rate`, `expected_value`, `expectancy`
- 관측 빈도는 과거 표본의 기술이며 다음 사건의 발생 가능성을 뜻하지 않는다.

**R-2. 방향을 판정하지 않는다.**
매매 방향을 함의하는 필드나 분기를 만들지 않는다. 이벤트는 사실과 측정값으로만 표현한다.

- **서술형 이름 규칙**: 방향이 있는 사실은 가격 기준의 위치와 변화로만 표현한다. `above`/`below`, `up`/`down`, `high`/`low`를 사용한다. 예: `break_side: "above_swing_high"`, 4분면 `oi_up_price_up`.
- 해석이 담긴 관용 라벨(롱 빌드업, 숏 커버링 등)을 값이나 필드명으로 쓰지 않는다.
- **금지어 목록**: 요약 JSON의 키와 문자열 값에 아래 단어를 snake_case 토큰으로 포함하지 않는다. T-6은 이 목록으로 검사한다.
  `signal`, `recommendation`, `recommend`, `bias`, `bullish`, `bearish`, `entry`, `exit`, `target`, `stop_loss`, `take_profit`, `probability`, `prob`, `win_rate`, `expected_value`, `confidence`, `score`, `support`, `resistance`
- 원천 데이터의 고유 명칭(`taker_buy_volume`, `taker_buy_sell_ratio` 등)은 방향 판정이 아니므로 허용한다.

**R-3. 결측을 채우지 않는다.**
보간, 전방 채우기, 0 대체, 직전값 복사를 수행하지 않는다. 결측은 `data_gap`에 기록하고 하위 계산에 결손 표시를 전파한다.

**R-4. 조정 가능한 수치를 코드에 기술하지 않는다.**
지표 기간, 임계값, 배수, 룩백, 요청 간격은 설정에서 읽는다. 함수 기본 인자에 매직 넘버를 넣지 않는다.

**R-5. 계층 경계를 넘지 않는다.**
3장의 의존 규칙을 따른다.

**R-6. 범위 밖 기능을 구현하지 않는다.**
`docs/PRD.md` 13장의 항목은 인터페이스 자리만 두고 구현하지 않는다. "나중에 쓸 것 같아서" 미리 만들지 않는다.

**R-7. 검증되지 않은 가중치를 만들지 않는다.**
여러 지표를 하나의 점수로 합성하는 로직을 작성하지 않는다. 레짐 확신도, 레벨 강도 점수, 통합 지수가 이에 해당한다.

**R-8. 인증이 필요한 엔드포인트를 사용하지 않는다.**
API 키를 코드, 설정, 환경변수로 취급하지 않는다.

---

## 3. 아키텍처 경계

### 3.1 계층

```
cli → report → compute → store
 └──────────────────────→ ingest → (외부)
공용: config, models
```

상위 계층은 하위 계층 어디든 참조할 수 있다. 역방향 참조를 금지한다. `ingest`와 `store`는 서로 참조하지 않으며, 둘 사이의 데이터 이동은 `cli`가 수행한다. 참조 가능 목록은 `docs/PRD.md` 7.2의 표를 따른다.

| 모듈 | 참조 가능 |
|---|---|
| `cli` | report, compute, store, ingest, config, models |
| `report` | compute, store, config, models |
| `compute` | store(조회 인터페이스), config, models |
| `store` | config, models |
| `ingest` | config, models |
| `config`, `models` | 표준 라이브러리만 |

### 3.2 계층별 책임

| 계층 | 책임 | 금지 |
|---|---|---|
| `cli` | 명령 해석, 흐름 조립, ingest 결과를 store에 적재 | 지표 계산, 파싱 |
| `ingest` | 아카이브 다운로드, REST 요청, 응답 파싱 | 지표 계산, 이벤트 판정, 저장소 접근 |
| `store` | 적재, 조회, 결측 관리 | 외부 요청, 지표 계산 |
| `compute` | 지표, 구조, 레짐, 레벨, 이벤트 | 외부 요청, 직접 파일 I/O |
| `report` | 요약 조립, 직렬화, 요약 파일 저장 | 지표 계산 |
| `config` | 설정 로드, 기본값, API 제약 상수 | 다른 모듈 참조 |
| `models` | 계층 간 데이터 구조 정의 | 다른 모듈 참조 |

### 3.3 출처 비인지 원칙

`compute` 이상은 데이터가 아카이브에서 왔는지 REST에서 왔는지 알지 못한다. `compute`는 `store`의 조회 인터페이스만 사용하며 `ingest`를 참조하지 않는다.

후속 버전에서 실시간 수집기를 추가할 때 `ingest`와 `store`만 확장되며, `compute` 이상은 변경되지 않아야 한다. 이 조건을 깨는 설계를 하지 않는다.

---

## 4. 데이터 취급 규칙

**D-1. 시각은 UTC 밀리초 정수다.**
저장, 연산, 비교에 정수를 사용한다. 부동소수점 변환을 거치지 않는다. 표시가 필요한 지점에서만 변환한다. 원본 형식은 데이터셋마다 다르다. klines·premiumIndexKlines는 밀리초 정수, metrics의 `create_time`은 `YYYY-MM-DD HH:MM:SS` UTC 문자열이다(`docs/PRD.md` 8.4).

**D-2. 아카이브 CSV는 정렬되어 있지 않다.**
특히 metrics 데이터셋은 시각 순서가 무작위다. 파싱 후 반드시 정렬한다. 파일 순서를 신뢰하지 않는다. 헤더 행 유무도 파일마다 다르므로 첫 줄을 판별한다.

**D-3. 중복은 기본키로 차단한다.**
시계열 테이블은 `(symbol, 시각)` 복합 기본키를 갖는다. 애플리케이션 레벨의 중복 검사에 의존하지 않는다.

**D-4. 없음과 0을 구분한다.**
값이 없으면 `null`이고 사유를 기록한다. 0으로 대체하지 않는다. 역으로 실제 0을 `null`로 표기하지 않는다.

**D-5. 미결제약정은 계약 수를 사용한다.**
`sum_open_interest`(계약 수)를 방향성 판단에 사용한다. `sum_open_interest_value`(명목)는 가격 변동만으로 증감하므로 방향성 지표로 사용하지 않는다. 두 값 모두 저장하되 용도를 혼동하지 않는다.

**D-6. 프리미엄 인덱스는 가격 지표다.**
`premiumIndexKlines`의 volume·taker 계열 컬럼은 항상 0이다. 거래량으로 해석하지 않는다. 사용 컬럼은 OHLC에 한정한다. 값은 음수를 가질 수 있다.

**D-7. 공개 체결량은 청산을 일부 제외한다.**
`aggTrades`는 보험기금 체결과 ADL 체결을 포함하지 않는다. 체결량을 "전체 거래량"으로 서술하거나 그렇게 가정하는 계산을 하지 않는다.

**D-8. 미완성 봉을 완성 봉과 구분한다.**
진행 중인 봉의 지표는 변경된다. 저장소에는 마감된 봉만 저장한다. 진행 중인 봉은 메모리에서만 사용하고, 요약에 `is_closed: false`로 전달한다.

**D-9. 결손은 전파한다.**
결측 구간을 포함해 계산된 지표는 결손 표시를 갖는다. 결손 표시는 계산 창 안의 결손 비율로 표기한다. 합성 봉은 원본 1분봉의 결측을 승계한다.

**D-10. metrics는 컬럼 단위로 병합한다.**
`metrics_5m`의 기존 행에서 NULL 칸만 새 값으로 채우고, NULL이 아닌 값은 바꾸지 않는다. 결측은 컬럼 단위로 탐지한다. 아카이브의 빈 문자열은 NULL이다. `ts`는 5분 구간의 끝 시각이며, 출처별 시각 보정은 `docs/PRD.md` 8.4를 따른다.

---

## 5. 외부 API 규칙

**A-1. 제약값은 상수 또는 설정으로 명시한다.**
요청 한도, 보관 기간, 창 크기 제한을 코드 각처에 산재시키지 않는다. 한 곳에서 정의하고 참조한다.

**A-2. 가중치를 추적한다.**
응답 헤더의 사용 가중치를 읽어 한도 초과를 방지한다. 설정된 비율에 도달하면 요청을 지연한다.

**A-3. 429를 준수한다.**
`Retry-After`를 따른다. 무시하고 재시도하지 않는다.

**A-4. 재시도 대상을 구분한다.**
네트워크 오류와 5xx는 지수 백오프로 재시도한다. 4xx(429 제외)는 재시도하지 않는다.

**A-5. 알려진 제약** (2026-09 기준, 변경 시 A-1에 따라 한 곳에서 수정)

| 대상 | 제약 |
|---|---|
| USD-M REST 전역 | IP당 분당 가중치 2400 |
| `/fapi/v1/klines` | 구현 상한 1000 (최대값 1000/1500 공식 확인 필요, `docs/PRD.md` 15.6) |
| `/fapi/v1/premiumIndexKlines` | 구현 상한 1000 (동일) |
| `/futures/data/*` | period 5m 이상, limit 최대 500, 최근 30일, IP당 5분간 1000회(가중치와 별개) |
| `/fapi/v1/premiumIndex` | 펀딩 정보 조회 전용, 가중치 공식 확인 필요 |
| `/fapi/v1/aggTrades` | 가중치 20, 24시간 이내, startTime/endTime 간격 1시간 미만, limit 최대 1000 |
| 아카이브 | UTC 일자 종료 후 공개, 1~2일 지연 가정 |

**A-6. 아카이브 무결성을 검증한다.**
`.CHECKSUM` 파일로 검증한다. 검증 실패 파일을 적재하지 않는다.

**A-7. 취득 불가를 우회하지 않는다.**
청산 데이터는 취득 경로가 없다. 대체 추정값을 만들어 채우지 않는다. `unavailable`로 표기한다.

---

## 6. 코딩 규약

**C-1.** Python 3.11 이상을 사용한다.

**C-2.** 의존 패키지를 최소화한다. 표준 라이브러리로 해결 가능한 것에 외부 패키지를 도입하지 않는다. 신규 의존성 추가 전에 질문한다.

**C-3.** 공개 함수와 데이터 구조에 타입 힌트를 부여한다.

**C-4.** 데이터 구조는 명시적으로 정의한다. 딕셔너리를 구조체 대용으로 계층 간 전달하지 않는다.

**C-5.** 예외를 광범위하게 포착하지 않는다. 포착한 예외는 기록하거나 변환하며, 무시하지 않는다.

**C-6.** 로깅은 표준 로깅 인터페이스를 사용한다. `print`를 로그 용도로 사용하지 않는다. CLI 출력과 로그를 분리한다.

**C-7.** 외부 요청은 엔드포인트, 파라미터 범위, 응답 상태, 소요 시간을 기록한다.

**C-8.** 함수는 단일 책임을 갖는다. 수집과 계산을 한 함수에서 수행하지 않는다.

---

## 7. 테스트 요구

**T-1. 지표 계산**
고정 입력에 대한 기대 출력으로 검증한다. 네트워크에 의존하지 않는다. 대상: ATR, Parkinson, ZigZag, BOS/MSS, 되돌림, 캔들 비율, KER, 봉 합성.

**T-2. 경계 조건**
데이터 부족, 전 구간 결측, 단일 봉, 동일가 연속 구간에서 예외 없이 동작하는지 검증한다.

**T-3. 수집 계층**
응답을 대체 가능한 형태로 구성하여 네트워크 없이 검증한다. 대상: 분할 요청, 재시도, 가중치 제어, 체크섬 실패 처리.

**T-4. 저장 계층**
중복 적재 멱등성, 결측 탐지 정확성, 결측 해소 시 갱신을 검증한다.

**T-5. 요약 생성**
전체 데이터 존재, 일부 결손, 전체 취득 실패 세 조건에서 요약이 생성되고 결손이 올바르게 표기되는지 검증한다.

**T-6. 금지 사항 검증**
요약 출력에 확률 계열 필드와 방향 판정 필드가 존재하지 않음을 R-2의 금지어 목록으로 검증한다. `statistics` 밖에 R-1의 이름 목록이 없음을 함께 검증한다(접미사 검사가 아니라 이름 검사다).

---

## 8. 작업 방식

**W-1. 불확실하면 질문한다.**
`docs/PRD.md`에 정의되지 않은 사항을 임의로 결정하지 않는다. 추정으로 구현하고 나중에 확인하는 방식을 사용하지 않는다.

**W-2. 큰 변경 전에 계획을 제시한다.**
여러 파일에 걸친 변경, 스키마 변경, 계층 구조 변경은 구현 전에 계획을 먼저 제시한다.

**W-3. 범위를 확장하지 않는다.**
요청된 작업만 수행한다. 관련 기능을 자발적으로 추가하지 않는다.

**W-4. 기존 구현을 임의로 재작성하지 않는다.**
리팩터링이 필요하다고 판단되면 제안하고 승인을 받는다.

**W-5. 검증되지 않은 외부 정보를 근거로 삼지 않는다.**
API 동작이 불확실하면 공식 문서를 확인하거나 질문한다. 기억에 의존해 엔드포인트나 제약을 단정하지 않는다.

---

## 9. 용어집

| 용어 | 정의 |
|---|---|
| 스윙 고점/저점 | ATR 기반 ZigZag로 확정된 파동의 극점 |
| 확정 파동 | 반전 임계값을 충족하여 극점이 확정된 파동 |
| 잠정 파동 | 진행 중이며 극점이 확정되지 않은 파동 |
| 구조 상태 | 최근 확정 스윙 고점 2개와 저점 2개의 대소(ATR 기준 허용 오차 안이면 같음)로 판정한 상태. higher_highs_higher_lows / lower_highs_lower_lows / mixed / insufficient (`docs/PRD.md` A.3.3) |
| BOS | 돌파 대상 스윙(유형별 가장 최근 확정 스윙, 돌파되면 대상 없음)을 구조 상태와 같은 방향으로 종가 돌파 |
| MSS | 구조 상태와 반대 방향으로 확정 스윙 극점을 변위 조건과 함께 종가 돌파 |
| 변위 | 돌파 봉의 몸통이 최근 N봉 평균 몸통의 설정 배수 이상인 상태 |
| 되돌림 깊이 | 직전 확정 파동 범위 대비 기준 가격의 역방향 진행 비율. 0~1로 자르지 않음 |
| 되돌림 시간 비율 | 조정 구간 봉 수 / 동인 파동 봉 수 |
| 레짐 | 두 개의 독립 축. 효율성 축(trend / range / transition, shock 활성 시 shock)과 변동성 축(expansion / normal / compression). 두 축을 합성하지 않음 |
| 충격 상태 | 큰 봉의 꼬리 비율 또는 짧은 간격 안의 양방향 돌파로 판정된 상태. 효율성 축만 덮어씀 |
| 4분면 | 계약 수 미결제약정과 가격의 증가·정체·감소 조합(3×3). 불감대는 분포 기준, 확정 상태는 지속 조건으로 갱신 |
| 체결 흐름 | 1분봉 taker 매수·매도 체결량의 차(델타). 공격적 체결 방향만 나타내며 OI의 롱·숏 구성은 나타내지 않음 |
| 레벨 구간 | 레벨 중심 ± 설정 배수 × ATR로 정의된 가격 구간 |
| 계열 | 지표의 정보 출처 분류. price_structure / regime / derivatives / level / flow |
| 결손 표시 | 계산 창 안의 결측 1분봉 비율(분 단위). 창에 부재 봉이 있으면 지표값은 `null` |
| 계획(plan) | 판단 모델이 낸 조건(activation, invalidation, objective)을 `plan/1` JSON으로 등록한 것. `side`는 입력을 되돌려 주는 값이며 프로그램의 판정이 아님 |
| 소급 평가 | 다음 실행 시 저장된 1분봉으로 계획의 충족 시각을 처음부터 다시 계산하는 방식. 상시 가동 없이 요약 사이의 공백을 메움 |
| MFE / MAE | 활성화 이후 최대 순행 / 최대 역행 거리(bp, ATR 배수) |
| 1초 버킷 | 체결 데이터를 1초 단위로 사전 집계한 레코드 (이번 버전 범위 밖) |

---

## 10. 참조

| 대상 | 위치 |
|---|---|
| 요구사항 정의 | `docs/PRD.md` |
| 기능 요구사항 | `docs/PRD.md` 10장 |
| 비기능 요구사항 | `docs/PRD.md` 11장 |
| 저장소 스키마 | `docs/PRD.md` 9장 |
| 범위 밖 항목 | `docs/PRD.md` 13장 |
| 완료 기준 | `docs/PRD.md` 14장 |
| 바이낸스 USD-M 선물 문서 | https://developers.binance.com/docs/derivatives/usds-margined-futures |
| 바이낸스 공개 데이터 아카이브 | https://data.binance.vision/ |
